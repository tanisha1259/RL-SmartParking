# Explored Q-Learning Configuration
# Higher learning rate and slower epsilon decay for more exploration
learning_rate: 0.15
discount_factor: 0.95
epsilon_start: 1.0
epsilon_min: 0.05
epsilon_decay: 0.995
episodes: 1000
model_name: "policy_v2_explored.pkl"
